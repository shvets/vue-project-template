import Vuex from 'vuex';

class AppStore extends Vuex.Store<any> {
  constructor(config: any) {
    super(config);
  }

  public setupStore() {}
}

export const state = {};

export const getters = {};

export const mutations = {};

export default AppStore;
